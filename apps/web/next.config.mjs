/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone',
  // Optional: if lint plugins ever fail CI, uncomment next line
  // eslint: { ignoreDuringBuilds: true },
};

export default nextConfig;
